const entrada = require('readline-sync');

const velocidade = entrada.question ("qual a velocidade d carro em km/H? ");

if (velocidade <= 80) {
    console.log("Velocidade dentro do limite da via, boa viagem!");
} else {
    console.log("velocidade acima da via, MULTADO")
}